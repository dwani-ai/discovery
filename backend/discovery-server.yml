services:
  fastapi:
    image: dwani/discovery-server:latest  # Replace with your FastAPI Docker image
    container_name: fastapi_server
    ports:
      - "8000:18888"  # Expose FastAPI server port (adjust if needed)
    environment:
      - DWANI_API_BASE_URL=${DWANI_API_BASE_URL}
      - DWANI_API_BASE_URL_LLM=${DWANI_API_BASE_URL_LLM:-${DWANI_API_BASE_URL}}
      - DWANI_LLM_MODEL=gemma4
      - DWANI_EMBEDDING_MODEL=google/embeddinggemma-300m
      - MAX_CONTEXT_TOKENS=6000
      - MAX_HISTORY_TOKENS=1000
      - DWANI_API_KEY=  #add -key -here  
    networks:
      - app-network
    extra_hosts:
      - "host.docker.internal:host-gateway"  # Allows access to host's localhost
    restart: unless-stopped

networks:
  app-network:
    driver: bridge